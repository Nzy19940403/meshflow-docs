[**MeshFlow Core API**](../README.md)

***

[MeshFlow Core API](../README.md) / BaseEngine

# Type Alias: BaseEngine\<T\>

> **BaseEngine**\<`T`\> = `object`

Defined in: [engine/useEngineManager.ts:13](https://github.com/Nzy19940403/meshflow/blob/851e3f10573edbcfafb6ad53b079e4487ddd4cd5/utils/core/engine/useEngineManager.ts#L13)

## Type Parameters

### T

`T`

## Properties

### config

> **config**: `object`

Defined in: [engine/useEngineManager.ts:20](https://github.com/Nzy19940403/meshflow/blob/851e3f10573edbcfafb6ad53b079e4487ddd4cd5/utils/core/engine/useEngineManager.ts#L20)

#### hasRenderGate

> **hasRenderGate**: `T` *extends* `object` ? `F` : `never`

#### notifyAll

> **notifyAll**: `T` *extends* `object` ? `F` : `never`

#### SetRule

> **SetRule**: `T` *extends* `object` ? `F` : `never`

#### SetRules

> **SetRules**: `T` *extends* `object` ? `F` : `never`

#### SetStrategy

> **SetStrategy**: `T` *extends* `object` ? `F` : `never`

#### SetTrace

> **SetTrace**: `T` *extends* `object` ? `F` : `never`

#### useEntangle

> **useEntangle**: `T` *extends* `object` ? `F` : `never`

#### usePlugin

> **usePlugin**: `T` *extends* `object` ? `F` : `never`

***

### data

> **data**: `object`

Defined in: [engine/useEngineManager.ts:14](https://github.com/Nzy19940403/meshflow/blob/851e3f10573edbcfafb6ad53b079e4487ddd4cd5/utils/core/engine/useEngineManager.ts#L14)

#### GetGroupByPath

> **GetGroupByPath**: `T` *extends* `object` ? `F` : `never`

#### GetValue

> **GetValue**: `T` *extends* `object` ? `F` : `never`

#### SetValue

> **SetValue**: `T` *extends* `object` ? `F` : `never`

#### SetValues

> **SetValues**: `T` *extends* `object` ? `F` : `never`

***

### dependency

> **dependency**: `object`

Defined in: [engine/useEngineManager.ts:32](https://github.com/Nzy19940403/meshflow/blob/851e3f10573edbcfafb6ad53b079e4487ddd4cd5/utils/core/engine/useEngineManager.ts#L32)

#### GetAllDependency

> **GetAllDependency**: `T` *extends* `object` ? `F` : `never`

#### GetDependencyOrder

> **GetDependencyOrder**: `T` *extends* `object` ? `F` : `never`

***

### hooks

> **hooks**: `object`

Defined in: [engine/useEngineManager.ts:36](https://github.com/Nzy19940403/meshflow/blob/851e3f10573edbcfafb6ad53b079e4487ddd4cd5/utils/core/engine/useEngineManager.ts#L36)

#### onError

> **onError**: `T` *extends* `object` ? `F` : `never`

#### onStart

> **onStart**: `T` *extends* `object` ? `F` : `never`

#### onSuccess

> **onSuccess**: `T` *extends* `object` ? `F` : `never`
